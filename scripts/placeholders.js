const Util = require('fallout-utility');

/**
 * 
 * @param {string} string - String to replace with placeholders
 * @param {Object[]} Bot - Mineflayer Bot object
 */
module.exports = (string, Bot) => {
    
    

    return string;
}